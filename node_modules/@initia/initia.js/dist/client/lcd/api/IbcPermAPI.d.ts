import { BaseAPI } from './BaseAPI';
import { APIParams, Pagination, PaginationOptions } from '../APIRequester';
export interface PermissionedRelayer {
    port_id: string;
    channel_id: string;
    relayer: string;
}
export declare class IbcPermAPI extends BaseAPI {
    relayers(params?: Partial<PaginationOptions & APIParams>): Promise<[PermissionedRelayer[], Pagination]>;
    relayer(port_id: string, channel_id: string): Promise<PermissionedRelayer>;
}
