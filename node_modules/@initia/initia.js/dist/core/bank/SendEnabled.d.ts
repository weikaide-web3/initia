import { JSONSerializable } from '../../util/json';
import { Denom } from '../Denom';
import { SendEnabled as SendEnabled_pb } from '@initia/initia.proto/cosmos/bank/v1beta1/bank';
export declare class SendEnabled extends JSONSerializable<SendEnabled.Amino, SendEnabled.Data, SendEnabled.Proto> {
    denom: Denom;
    enabled: boolean;
    /**
     * @param denom
     * @param enabled
     */
    constructor(denom: Denom, enabled: boolean);
    static fromAmino(data: SendEnabled.Amino): SendEnabled;
    toAmino(): SendEnabled.Amino;
    static fromData(data: SendEnabled.Data): SendEnabled;
    toData(): SendEnabled.Data;
    static fromProto(data: SendEnabled.Proto): SendEnabled;
    toProto(): SendEnabled.Proto;
}
export declare namespace SendEnabled {
    interface Amino {
        denom: Denom;
        enabled: boolean;
    }
    interface Data {
        denom: Denom;
        enabled: boolean;
    }
    type Proto = SendEnabled_pb;
}
