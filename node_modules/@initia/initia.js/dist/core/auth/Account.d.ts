import { Any } from '@initia/initia.proto/google/protobuf/any';
import { BaseAccount } from './BaseAccount';
import { ModuleAccount } from './ModuleAccount';
export type Account = BaseAccount | ModuleAccount;
/**
 * Stores information about an account fetched from the blockchain.
 */
export declare namespace Account {
    type Amino = BaseAccount.Amino | ModuleAccount.Amino;
    type Data = BaseAccount.Data | ModuleAccount.Data;
    type Proto = Any;
    function fromAmino(amino: Account.Amino): Account;
    function fromData(data: Account.Data): Account;
    function fromProto(accountAny: Account.Proto): Account;
}
