import { JSONSerializable } from '../../util/json';
import { Coins } from '../Coins';
import { AccAddress } from '../bech32';
import { Params as Params_pb } from '@initia/opinit.proto/opinit/opchild/v1/types';
export declare class OpchildParams extends JSONSerializable<OpchildParams.Amino, OpchildParams.Data, OpchildParams.Proto> {
    max_validators: number;
    historical_entries: number;
    bridge_executor: AccAddress;
    admin: AccAddress;
    fee_whitelist: string[];
    min_gas_prices: Coins;
    /**
     * @param max_validators the maximum number of validators
     * @param historical_entries the number of historical entries to persist
     * @param min_gas_prices
     * @param bridge_executor the account address of bridge executor who can execute permissioned bridge messages
     * @param admin the account address of admin who can execute permissioned cosmos messages
     * @param fee_whitelist the list of addresses that are allowed to pay zero fee
     */
    constructor(max_validators: number, historical_entries: number, min_gas_prices: Coins.Input, bridge_executor: AccAddress, admin: AccAddress, fee_whitelist: string[]);
    static fromAmino(data: OpchildParams.Amino): OpchildParams;
    toAmino(): OpchildParams.Amino;
    static fromData(data: OpchildParams.Data): OpchildParams;
    toData(): OpchildParams.Data;
    static fromProto(data: OpchildParams.Proto): OpchildParams;
    toProto(): OpchildParams.Proto;
}
export declare namespace OpchildParams {
    interface Amino {
        type: 'opchild/Params';
        value: {
            max_validators: number;
            historical_entries: number;
            min_gas_prices: Coins.Amino;
            bridge_executor: AccAddress;
            admin: AccAddress;
            fee_whitelist: string[];
        };
    }
    interface Data {
        '@type': '/opinit.opchild.v1.Params';
        max_validators: number;
        historical_entries: number;
        min_gas_prices: Coins.Data;
        bridge_executor: AccAddress;
        admin: AccAddress;
        fee_whitelist: string[];
    }
    type Proto = Params_pb;
}
