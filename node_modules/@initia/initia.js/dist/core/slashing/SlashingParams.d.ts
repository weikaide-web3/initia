import { JSONSerializable } from '../../util/json';
import { Duration } from '../Duration';
import { Params as Params_pb } from '@initia/initia.proto/cosmos/slashing/v1beta1/slashing';
export declare class SlashingParams extends JSONSerializable<SlashingParams.Amino, SlashingParams.Data, SlashingParams.Proto> {
    signed_blocks_window: number;
    min_signed_per_window: number;
    downtime_jail_duration: Duration;
    slash_fraction_double_sign: number;
    slash_fraction_downtime: number;
    /**
     * @param signed_blocks_window Number of blocks over which missed blocks are tallied for downtime
     * @param min_signed_per_window If a validator misses more than this number, they will be penalized and jailed for downtime
     * @param downtime_jail_duration Amount of time in seconds after which a jailed validator can be unjailed
     * @param slash_fraction_double_sign Ratio of funds slashed for a double-sign infraction
     * @param slash_fraction_downtime Ratio of funds slashed for a downtime infraction
     */
    constructor(signed_blocks_window: number, min_signed_per_window: number, downtime_jail_duration: Duration, slash_fraction_double_sign: number, slash_fraction_downtime: number);
    static fromAmino(data: SlashingParams.Amino): SlashingParams;
    toAmino(): SlashingParams.Amino;
    static fromData(data: SlashingParams.Data): SlashingParams;
    toData(): SlashingParams.Data;
    static fromProto(data: SlashingParams.Proto): SlashingParams;
    toProto(): SlashingParams.Proto;
}
export declare namespace SlashingParams {
    interface Amino {
        type: 'cosmos-sdk/x/slashing/Params';
        value: {
            signed_blocks_window: string;
            min_signed_per_window: string;
            downtime_jail_duration: Duration.Amino;
            slash_fraction_double_sign: string;
            slash_fraction_downtime: string;
        };
    }
    interface Data {
        '@type': '/cosmos.slashing.v1beta1.Params';
        signed_blocks_window: string;
        min_signed_per_window: string;
        downtime_jail_duration: Duration.Data;
        slash_fraction_double_sign: string;
        slash_fraction_downtime: string;
    }
    type Proto = Params_pb;
}
